# Gradle — кэш и сборки (тяжело; `./gradlew` остаётся в репо)
.gradle/
**/local.properties
**/captures/
.cxx/
**/build/

# Подпись релиза — не коммитить
key.properties
**/*.keystore
**/*.jks

# Регенерируется Flutter
GeneratedPluginRegistrant.java
